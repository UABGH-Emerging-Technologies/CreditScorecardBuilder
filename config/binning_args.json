{
  "// Comments": "Configuration for OptBinning BinningProcess",
  "// Note": "Use null for package default values",
  "// Documentation": "https://optbinning.readthedocs.io/en/stable/binning_process.html",
  
  "max_n_prebins": 30,
  "// max_n_prebins_comment": "The maximum number of bins after pre-binning (default: 20)",
  
  "min_prebin_size": 0.01,
  "// min_prebin_size_comment": "The fraction of minimum records for each prebin (default: 0.05 = 5%)",
  
  "min_n_bins":2,
  "// min_n_bins_comment": "The minimum number of bins per variable (default: null)",
  
  "max_n_bins": 10,
  "// max_n_bins_comment": "The maximum number of bins per variable (default: null)",
  
  "min_bin_size": 0.005,
  "// min_bin_size_comment": "The fraction of minimum records for each bin (default: null = min_prebin_size)",
  
  "max_bin_size": 0.5,
  "// max_bin_size_comment": "The fraction of maximum records for each bin (default: null = 1.0)",
  
  "max_pvalue": 0.1,
  "// max_pvalue_comment": "The maximum p-value among bins (default: null)",
  
  "max_pvalue_policy": "consecutive",
  "// max_pvalue_policy_comment": "Method for p-value constraint: 'consecutive' or 'all' (default: 'consecutive')",
  
  "selection_criteria": null,
  "// selection_criteria_comment": "Variable selection criteria dict (default: null = no selection)",
  
  "verbose": true,
  "// verbose_comment": "Enable verbose output for debugging (default: false)",
  
  "fuzz_catch_asa_category": true,
  "// fuzz_catch_asa_category_comment": "Automatically detect ASA status variables and treat as categorical",
  
  "asa_detection": {
    "name_contains": "asa",
    "// name_contains_comment": "Variable name must contain this substring (case-insensitive)",
    
    "max_unique_values": 6,
    "// max_unique_values_comment": "Maximum unique values to consider as ASA status (ASA I-VI)"
  }
}